// Use plugin names (strings) so Next's Webpack build and Turbopack both accept the PostCSS config.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
