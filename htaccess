# Apache .htaccess configuration for API subdomain
# Redirects ALL requests from api.timeliteclothing.com to port 3002 (Gateway)

# Enable Rewrite Engine
RewriteEngine On

# Proxy ALL requests to port 3002 (Gateway)
# Gateway handles routing to PHP and Node backends
# Gateway (Nginx) also handles CORS headers, so we don't add them here to avoid duplicates
RewriteCond %{REQUEST_URI} !^/\.well-known [NC]
RewriteRule ^(.*)$ http://127.0.0.1:3002%{REQUEST_URI} [P,L,QSA]
